import type {Metadata} from "next";import Link from "next/link";import {kurslar} from "@/lib/kultur";import {sunucuSupabase} from "@/lib/supabase/server";
export const metadata:Metadata={title:"Bağlama, Ney ve Halk Müziği Öğrenme Rehberi",description:"Bağlama ve ney kurslarını seçerken dikkat edilmesi gerekenler, seviyeye göre çalışma yolları ve halk müziği eğitim programları.",alternates:{canonical:"/kurslar"}};
export default async function Kurslar(){let ek:any[]=[];try{const db=await sunucuSupabase();const{data}=await db.from("kultur_icerikleri").select("slug,baslik,ozet,icerik,dis_url,meta").eq("tur","kurs").eq("durum","yayinda");ek=data??[]}catch{}return <main className="mx-auto max-w-6xl px-4 py-10"><header className="max-w-3xl"><p className="text-xs font-bold uppercase tracking-[.22em] text-kilim">Dinlemekten icraya</p><h1 className="mt-2 font-serif text-4xl font-semibold text-ceviz sm:text-5xl">Bağlama ve ney öğrenme yolları</h1><p className="mt-4 text-lg leading-8 text-ceviz-light">Doğru eğitmeni, seviyeyi ve çalışma yöntemini seçmenize yardımcı olan bağımsız rehber. Sponsorlu kurslar ayrıca ve açıkça işaretlenir.</p></header><section className="mt-9 grid gap-5 md:grid-cols-2">{kurslar.map((k,i)=><article key={k.slug} className="relative overflow-hidden rounded-3xl border border-toprak/25 bg-white/55 p-6 shadow-sm"><span className="absolute right-5 top-4 font-serif text-5xl text-toprak/15">{i+1}</span><p className="text-xs font-bold uppercase tracking-wider text-cini-dark">{k.alan} · {k.seviye}</p><h2 className="mt-2 font-serif text-2xl font-semibold text-ceviz">{k.baslik}</h2><p className="mt-2 text-sm leading-6 text-ceviz-light">{k.ozet}</p><ul className="mt-4 space-y-2 text-sm text-ceviz">{k.kazanimlar.map(x=><li key={x}>◇ {x}</li>)}</ul><p className="mt-5 text-xs font-semibold text-kilim">{k.format}</p></article>)}</section>{ek.length>0&&<section className="mt-12"><h2 className="font-serif text-3xl font-semibold text-ceviz">Doğrulanmış eğitim duyuruları</h2><div className="mt-5 grid gap-4 md:grid-cols-2">{ek.map(k=><article key={k.slug} className="rounded-2xl border border-toprak/25 p-5"><p className="text-[10px] font-bold uppercase tracking-wider text-kilim">Dış kurum içeriği</p><h3 className="mt-2 font-serif text-xl font-semibold">{k.baslik}</h3><p className="mt-2 text-sm text-ceviz-light">{k.ozet}</p>{k.dis_url&&<a href={k.dis_url} rel="sponsored noopener" target="_blank" className="mt-4 inline-block text-sm font-semibold text-cini-dark underline">Kurum sayfasını aç →</a>}</article>)}</div></section>}<section className="mt-12 rounded-3xl bg-ceviz p-7 text-parsomen"><h2 className="font-serif text-2xl font-semibold">Eğitmen veya kurum musunuz?</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-parsomen/65">Programınız kaynaklı halk müziği eğitimi, şeffaf ücret ve açık eğitmen bilgisi sunuyorsa rehbere değerlendirilmek üzere başvurabilirsiniz. Ücretli yerleşimler editoryal sıralamayı satın alamaz.</p><Link href="/is-birligi" className="mt-5 inline-flex rounded-xl bg-parsomen px-5 py-3 font-semibold text-ceviz">İş birliği ilkelerini incele</Link></section></main>}
